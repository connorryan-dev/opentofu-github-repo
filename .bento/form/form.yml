name: "Create GitHub Repository"
description: "Create a new GitHub repository under the org following org standards"
body:
  - type: text
    id: repo_name
    attributes:
      label: "Repository Name"
      description: "Lowercase with hyphens (e.g. my-service)."
      placeholder: "my-service"
    validations:
      required: true

  - type: text
    id: repo_description
    attributes:
      label: "Description"
      description: "Short description of the repository."
      placeholder: "Service for handling payments"
    validations:
      required: false

  - type: select
    id: visibility
    attributes:
      label: "Visibility"
      options:
        - label: "Private"
          value: "private"
        - label: "Public"
          value: "public"
    validations:
      required: true

  - type: boolean
    id: has_issues
    attributes:
      label: "Enable Issues"

  - type: boolean
    id: has_wiki
    attributes:
      label: "Enable Wiki"
